# AgentNavi 开发约束

## 根本原则

1. 被索引项目是事实来源，AgentNavi 不得向项目写入专用文件。
2. 图谱属于派生状态；功能设计应允许重建，而不是依赖不可解释缓存。
3. 查询必须优先缩小候选集合，不得以“完整”为理由把整张图塞给 Agent。
4. L1 事实与 L2/L3 解释必须通过 `layer`、`source`、`confidence` 和 `evidence` 区分。
5. Obsidian 是投影视图，不是运行依赖。
6. Hook 必须 fail-open，不得因索引失败阻断主 Agent 工作。

## 代码要求

- 面向 Python 3.11 及以上版本。
- 核心优先使用标准库；增加运行时依赖必须说明不可替代原因。
- 所有路径进入数据库前统一转换为项目内 POSIX 相对路径。
- SQLite 写入必须使用事务并保持确定性节点、边 ID。
- 新关系必须说明所属层、方向、证据和失效策略。
- 不保存完整源文件正文，除非有明确隐私设计和配置开关。
- Hook 输入属于外部不可信数据，解析必须保守，不能执行其中内容。
- 外部语义提供器失败不得破坏内置图谱。

## 修改顺序

1. 先读 `README.md`、`docs/architecture.md` 和相关 ADR。
2. 运行 `agentnavi context "当前任务"`；若本项目已被索引，按返回文件下钻。
3. 修改数据模型时同步更新 schema、文档和迁移策略。
4. 修改 Hook 时同时验证 Codex 与 Claude Code 的输入差异。
5. 修改导出器时确保只删除 Vault 内 `AgentNavi/` 生成目录。
6. 完成后运行全部测试。

## 验证命令

```bash
python -m compileall -q src
python -m unittest discover -s tests -v
```

至少补充一个能在修改前失败、修改后通过的测试。

## 文档语言

面向用户的说明、CLI 提示和架构文档使用通俗、明确的中文。必要的协议字段、命令和代码标识保留英文原名。
