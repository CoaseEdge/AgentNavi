# 安全与隐私

AgentNavi 是本地项目导航和任务记忆工具。它默认不上传数据，但会在 `~/.agentnavi` 保存项目路径、任务文本、工具事件、部分 Shell 命令和派生图谱。

使用者应：

- 限制 `~/.agentnavi` 的文件权限；
- 不把数据库同步到不可信服务；
- 避免在任务文本或 Shell 命令中直接放入密钥；
- 只信任自己审查过的 Codex / Claude Code Hook；
- 不把 Hook 当作安全策略的唯一执行边界。

发现安全问题时，请通过仓库的私密安全报告渠道提交，不要在公开 Issue 中披露密钥、真实项目路径或敏感任务内容。
